// Dynamic group id — can't be prerendered (ids unknown at build). Rendered
// client-side like the rest of the SPA; the shell is served via the fallback.
export const prerender = false;
export const ssr = false;
